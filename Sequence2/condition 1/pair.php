<?php

// demander la saisie d'un nombre puis afficher si le nombre est pair ou impair

$saisie = readline("Entrez un nombre : ");

if ($saisie%2==0) {
    echo "$saisie";
} else {
    echo "$saisie";
}